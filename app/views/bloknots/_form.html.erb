<%= form_for(@bloknot) do |f| %>
  <% if @bloknot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bloknot.errors.count, "error") %> prohibited this bloknot from being saved:</h2>

      <ul>
      <% @bloknot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :NameOfBkoknot %><br>
    <%= f.text_field :NameOfBkoknot %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
